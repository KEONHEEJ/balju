# 발주판 (옛 정적판)

**이 저장소는 더 이상 쓰지 않는다.** 발주판은 Next.js + Supabase 앱으로 옮겼다.

- 새 주소: https://quanandao-balju.vercel.app
- 새 코드: `~/Desktop/콴안다오/balju-app`

여기 남은 `index.html` 은 예전 주소로 들어온 사람을 새 주소로 넘기는 일만 한다.
홈 화면에 아이콘을 둔 기기가 죽지 않도록 지우지 않고 남겨둔다.
`sw.js` 는 예전 오프라인 캐시를 비우고 스스로 물러난다.
